{
  "name": "user-dashboard-app",
  "version": "1.0.0",
  "description": "Full-stack user authentication dashboard with Express, MongoDB, and JWT.",
  "private": true,
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "start": "npm start --prefix backend",
    "check": "npm run check --prefix backend && node --check api/index.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.4.1"
  }
}
